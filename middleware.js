// OG tag routing is handled by vercel.json `has` conditional rewrites.
// This file is intentionally a no-op passthrough.
export const config = { matcher: [] };
export default function middleware() {}
